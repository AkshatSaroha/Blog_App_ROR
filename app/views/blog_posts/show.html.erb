<div class="bg-white">
    <div class="flex justify-center mb-4 ">
      <%= button_to "Edit", blog_post_edit_path, method: :get, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" if user_signed_in? %>
    </div>
    <div class="max-w-3xl mx-auto bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
      <h2 class="text-3xl font-bold text-center mb-4"><%= @blog_post.title %></h2>
      <p class="text-lg text-center mb-8"><%= @blog_post.body %></p>
    </div>
  </div>